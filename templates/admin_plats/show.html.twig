{% extends 'base.html.twig' %}

{% block title %}Plats{% endblock %}

{% block body %}
    <h1>Plats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ plat.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ plat.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_plats_index') }}">back to list</a>

    <a href="{{ path('app_admin_plats_edit', {'id': plat.id}) }}">edit</a>

    {{ include('admin_plats/_delete_form.html.twig') }}
{% endblock %}
